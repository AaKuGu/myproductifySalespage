import React from "react";

export const metadata = {
  title: "Jv Page",
  description: "My Productify Jv Page",
};

const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
